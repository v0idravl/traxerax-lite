{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Traxerax Lite",
            "type": "python",
            "request": "launch",
            "module": "traxerax_lite.main",
            "cwd": "${workspaceFolder}",
            "env": {
                "PYTHONPATH": "${workspaceFolder}/src"
            },
            "console": "integratedTerminal"
        }
    ]
}